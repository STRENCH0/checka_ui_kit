library checka_ui_kit;

import 'package:flutter/widgets.dart';

const DEFAULT_BORDER_RADIUS = 24.0;
const DEFAULT_PADDING = 8.0;
const TRANSPARENT_COLOR = Color(0x44000000);

BoxConstraints get defaultInputConstraints =>
    BoxConstraints.tightFor(height: 45.0, width: 80.0);
